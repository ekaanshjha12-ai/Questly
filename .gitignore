node_modules
dist
.DS_Store
*.local
.env
server/data

# Blender working files. The .py scripts are the real source; the .glb and .png
# outputs are regenerated by them, and the shipped copies live in public/models.
blender/*.glb
blender/*.png

# Scratch mocks used when testing against a fake Anthropic endpoint.
*mock*.mjs

# TypeScript incremental build cache — regenerated on every build.
*.tsbuildinfo
